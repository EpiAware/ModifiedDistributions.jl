# MANAGED by EpiAwarePackageTools.scaffold — do not edit by hand.
name: Documenter

on:
  push:
    branches: main
    tags: '*'
  pull_request:
  merge_group:
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  Documenter:
    uses: EpiAware/.github/.github/workflows/documentation.yml@383486033b4ae93561695411c9033f204df75d12
    secrets: inherit  # pragma: allowlist secret
